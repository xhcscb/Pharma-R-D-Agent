# 合成测试数据

本目录中的文件均为团队编写的合成测试数据，可以提交 Git。它们用于验证：

- 官方新闻 HTML 解析；
- 临床 JSON 完整保留；
- 财务 XBRL/XML 事实和单元格证据；
- 电话会议文字稿解析；
- 实体提及、候选关系、证据绑定和可视化接口。

合成数据只能证明流程和契约可运行，不能替代真实 Gold 语料的准确率验收。二进制 PDF、录音和模型权重不会存入 Git；需要这些输入时，测试会在临时目录中生成最小化二进制样例。
