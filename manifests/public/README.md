# Public manifests

Only source metadata and URLs that are safe to distribute belong here.

- Never commit copyrighted brokerage-report PDFs.
- Never commit private or unlicensed call recordings.
- Keep license_status and access_class explicit.
- Use manifests/examples as field templates.
- A public dataset snapshot rejects every non-public document version.
